# Core ML / separation backends
# NOTE: install the CUDA build of torch FIRST (see README), e.g.:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
torch>=2.1
torchaudio>=2.1

# Separation engines
demucs>=4.0
audio-separator>=0.18
# audio-separator imports onnxruntime at load time. Install the GPU build to run
# MDX/VR models on CUDA (Roformer/Demucs use torch directly). CPU-only machines
# can use plain `onnxruntime` instead.
onnxruntime-gpu>=1.17

# Audio I/O
numpy>=1.24,<2.0
soundfile>=0.12

# CLI / UX
typer>=0.12
rich>=13.0

# Dev / tests
pytest>=8.0
